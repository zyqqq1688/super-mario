export const GRAVITY = 0.6;
export const FRICTION = 0.8;
export const MOVE_SPEED = 3;
export const JUMP_FORCE = -17;
export const BOUNCE_FORCE = -8; // Force when jumping on enemy
export const TERMINAL_VELOCITY = 15;

export const WORLD_HEIGHT = 600;
export const SCREEN_WIDTH = 1000; // Conceptual width for calculation
export const TILE_SIZE = 40;

export const PLAYER_SIZE = { width: 32, height: 32 };

// Initial seed for map generation (static for this example, but logic is dynamic)
export const INITIAL_LEVEL_WIDTH = 3000;